<h2>Posts</h2>
<ul class="posts">
  <% posts.each(function(post){ %>
    <li><%= post.escape('title') %>
    <button 
      data-id="<%= post.id %>"
      class="delete">
    Delete
    </button></li>
    <% }); %>
</ul>

